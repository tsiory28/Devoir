#include <stdio.h>
int main(){
	printf("Le jeu consiste a faire deviner au programme un chiffre entre 0 a 100\n");
	int n=100;
/*	int x;
	char reps;
	while(n!=b){nf
	}
	*/
	/*int inf=0,sup=100,mil,cpt=0
	 int reponse;
	 while("fabs(inf+sup)/2;
	 * printf("le nombre est il egal a %d:(1/oui et 2/non)",mil);
	   scanf("%d",&reponse);
	   if(reponse){
		printf("le nobmre est il inferieur a %d:(1/oui et 2/non)",(mil+1));
		scanf("%d",&reponse);
		if(reponse){
		   break;
		    }
		    else{
		          inf = mil;
		          }
		     }
		     else{
		            sup = mil;
		             }
		             
		     printf("Le nombre est egale a %d\nombre de tentative : %d",mil,cpt);
		                  
	*/
	int b;
	b = n/2;
	
	printf("le chiffre est il %d ?\n",n);
		//scanf("%c",reps);
		printf("le nombre est il supp a %d\n",b);
		
	return 0;
}
